"multdrc" <- function(
formula, curve, collapse, weights, data = NULL, boxcox = FALSE, bcAdd = 0, varPower = FALSE, 
startVal, fct = LL.4(), na.action = na.fail, hetvar = NULL, robust = "mean", type = "continuous", 
cm = NULL, logDose = NULL, fctList = NULL, control = NULL, sidestep = FALSE)
{
    stop("multdrc() has been taken out of use as of version 1.6-0. Use drm() instead.")
}
    